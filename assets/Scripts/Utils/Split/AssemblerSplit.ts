import { _decorator, Component, IAssembler, Node } from 'cc';
const { property } = _decorator;

export namespace Utils.Split {
    export class AssemblerSplit implements IAssembler {

        // createData(com: SplitRen);
    }
}

